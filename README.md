# designlib-mcp

MCP server exposing a curated design-knowledge catalog (web + iOS).

## Development

```bash
python -m venv .venv
source .venv/Scripts/activate  # or .venv/bin/activate on POSIX
pip install -e ".[dev]"
pytest
```
